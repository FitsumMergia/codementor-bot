services:
  bot:
    build: .
    restart: always
    env_file: .env
    volumes:
      - ./data:/app/data
    environment:
      - DATABASE_URL=sqlite:///./data/codementor.db
      - PYTHONUNBUFFERED=1
